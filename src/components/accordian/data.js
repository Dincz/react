const data = [
  {
    "id": "1",
    "question": "What is the capital of France?",
    "answer": "The reason you use an arrow function in the onClick event handler, like onClick={() => singleClick(dataItem.id)}, is to prevent the function from being immediately invoked when the component renders."
  },
  {
    "id": "2",
    "question": "What is the largest country in the world by land area?",
    "answer": "The reason you use an arrow function in the onClick event handler, like onClick={() => singleClick(dataItem.id)}, is to prevent the function from being immediately invoked when the component renders."
  },
  {
    "id": "3",
    "question": "What is the tallest mountain in the world?",
    "answer": "Mount The reason you use an arrow function in the onClick event handler, like onClick={() => singleClick(dataItem.id)}, is to prevent the function from being immediately invoked when the component renders."
  },
  {
    "id": "4",
    "question": "What is the most populous city in the world?",
    "answer": "Tokyo The reason you use an arrow function in the onClick event handler, like onClick={() => singleClick(dataItem.id)}, is to prevent the function from being immediately invoked when the component renders."
  }
]

export default data;


